# Simplatab — pinned dependency set.
#
# Pins justified:
#   * numpy 1.23.5  -> required by featurewiz 0.3.2 and matches the lower
#                      bound of pandas 2.0.3 / scikit-learn 1.3.1.
#   * pandas 2.0.3 -> last release that still works with featurewiz 0.3.2.
#   * scikit-learn 1.3.1 -> stable API used throughout pipelines / shap.
#   * scipy 1.10.1 -> compatible with sklearn 1.3.1 and numpy 1.23.5.
#   * shap 0.43.0 -> first version that handles multiclass cleanly.
#   * xgboost 1.7.6 -> compatible with sklearn 1.3 and numpy 1.23.
#   * lightgbm 4.1.0 -> CPU build; pulled in by featurewiz transitively.
#   * featurewiz 0.3.2 -> caps pandas/numpy upgrades; treat as anchor.
#   * matplotlib 3.7.3 + matplotlib-inline 0.1.6 -> avoid the 3.8 API breaks.
#   * seaborn 0.13.0 -> last version that supports mpl 3.7.x.
#   * MiniSom 2.3.2 -> consumed by Helpers.DBDM for clustering.
#   * pyarrow 16.0.0 -> parquet readers / faster CSV.
#   * openpyxl 3.1.2 -> used by MetricsReport excel writers.
#   * flask 3.0.3 -> Flask UI.
#   * Werkzeug 3.0.3 -> pinned to match flask 3.0.3 (avoids
#                       DispatcherMiddleware.import-path drift in 3.1).
#   * PyYAML >=6 -> reads machine_learning_parameters.yaml.
#   * python-dateutil 2.8.2 -> avoids the 2.9 'six' regression.
#   * six 1.16.0 -> backstop for the dateutil regression.
#
# Removed:
#   * docker==7.1.0 -> not imported anywhere in the codebase.
#
lightgbm==4.1.0
numpy==1.23.5
pandas==2.0.3
pyarrow==16.0.0
openpyxl==3.1.2
scikit-learn==1.3.1
scipy==1.10.1
seaborn==0.13.0
shap==0.43.0
matplotlib==3.7.3
matplotlib-inline==0.1.6
xgboost==1.7.6
featurewiz==0.3.2
flask==3.0.3
Werkzeug==3.0.3
MiniSom==2.3.2
PyYAML>=6.0,<7
python-dateutil==2.8.2
six==1.16.0
